# Work Log - 2026-04-25

## Branch

`feature/gas-build-setup`

## Task Summary

- `Build.cs`에 GAS 핵심 모듈 의존성을 추가했습니다.
- `uproject`에 `GameplayAbilities` 플러그인을 활성화했습니다.
- 변경 후 에디터 타깃 빌드 성공 여부를 확인했습니다.

## Files Changed

- `Source/OVERDRIVE/OVERDRIVE.Build.cs`: GAS 사용을 위한 `GameplayAbilities`, `GameplayTags`, `GameplayTasks` 모듈 의존성 추가
- `OVERDRIVE.uproject`: `GameplayAbilities` 플러그인 활성화
- `Docs/WorkLogs/2026-04-25 - GAS 빌드 기반 구성.md`: 이번 채팅 작업 로그 기록

## Implementation Summary

- 기존 `OVERDRIVE` 모듈은 `Core`, `CoreUObject`, `Engine`, `InputCore`, `EnhancedInput`만 참조하고 있었습니다.
- GAS 기반 C++ 작업을 시작할 수 있도록 Public Dependency Module에 `GameplayAbilities`, `GameplayTags`, `GameplayTasks`를 추가했습니다.
- 이후 플러그인 활성화 누락으로 인한 에디터/프로젝트 설정 불일치를 방지하기 위해 `OVERDRIVE.uproject`에 `GameplayAbilities` 플러그인을 명시적으로 활성화했습니다.
- Unreal Build Tool로 `OVERDRIVEEditor Win64 Development` 빌드를 실행해 실제 컴파일 성공을 검증했습니다.

## Networking Impact

- 서버 / 클라이언트 책임
  - 이번 작업은 빌드 설정 단계라 런타임 책임 분리는 변경되지 않았습니다.
- Replication 영향
  - 없음
- RPC 영향
  - 없음
- Dedicated Server 관련 사항
  - 없음. 다만 향후 Dedicated Server 기반 GAS 구조를 추가하기 위한 선행 작업입니다.

## GAS Impact

- Ability 변경
  - 없음
- GameplayEffect 변경
  - 없음
- GameplayTag 변경
  - 없음
- Attribute 변경
  - 없음
- Grant / Remove 변경
  - 없음
- 추가 사항
  - GAS 관련 클래스를 C++에서 사용할 수 있는 모듈 기반을 준비했습니다.

## Problems

- 첫 빌드 시 `UnrealBuildTool` 로그 파일 처리 과정에서 권한 오류가 발생했습니다.

## Decisions / Solutions

- `Build.cs`에 GAS 핵심 모듈 3종을 한 번에 추가했습니다.
  - 이유: GAS 도입 초기에 일반적으로 함께 사용하는 최소 단위이기 때문입니다.
- `uproject`에 `GameplayAbilities` 플러그인을 함께 활성화했습니다.
  - 이유: Build.cs만 수정하는 것보다 프로젝트 설정까지 명시해 두는 편이 이후 작업과 협업 시 더 설명 가능하고 안정적이기 때문입니다.
- 권한 확장 후 빌드를 다시 실행해 실제 컴파일 성공을 확인했습니다.
  - 이유: 이번 작업 완료 기준에 컴파일 검증이 중요했기 때문입니다.

## TODO

- ASC Ownership 위치 결정
- `AbilitySystemInterface` 도입 위치 결정
- PlayerState 기반 ASC 여부 검토
- Character Avatar 초기화 흐름 설계
- 초기 GameplayTag / AttributeSet 구조 정의

## Portfolio Summary

- GAS 도입을 위해 Unreal 모듈 의존성과 프로젝트 플러그인 설정을 정리하고, 실제 에디터 빌드 성공까지 검증했습니다.
